const MELON='https://www.melon.com/chart/index.htm';
const clean=s=>s.replace(/<[^>]*>/g,' ').replace(/&nbsp;/g,' ').replace(/&amp;/g,'&').replace(/&#39;/g,"'").replace(/&quot;/g,'"').replace(/\s+/g,' ').trim();
const pick=(html,re)=>{const match=html.match(re);return match?clean(match[1]):''};
function parse(html){const chartTime=[pick(html,/<span class="year">([\s\S]*?)<\/span>/i),pick(html,/<span class="hour">([\s\S]*?)<\/span>/i)].filter(Boolean).join(' ');const rows=html.match(/<tr[^>]+class="[^"]*lst(?:50|100)[^"]*"[\s\S]*?<\/tr>/gi)||[];const songs=rows.map((row,index)=>{const rank=Number(pick(row,/<span class="rank">(\d+)<\/span>/i))||index+1;const title=pick(row,/class="ellipsis rank01"[\s\S]*?<a[^>]*>([\s\S]*?)<\/a>/i);const artist=pick(row,/class="ellipsis rank02"[\s\S]*?<a[^>]*>([\s\S]*?)<\/a>/i);const album=pick(row,/class="ellipsis rank03"[\s\S]*?<a[^>]*>([\s\S]*?)<\/a>/i);const image=((row.match(/<img[^>]+src="([^"]+)"/i)||[])[1]||'').replace(/^\/\//,'https://');const change=Number(pick(row,/<span class="(?:up|down)">([\s\S]*?)<\/span>/i))||0;const status=/class="up"/i.test(row)?'up':/class="down"/i.test(row)?'down':/class="new"/i.test(row)?'new':'same';return{rank,title,artist,album,image,change,status}}).filter(song=>song.title&&song.artist).slice(0,100);return{chartTime,songs}}
async function chart(request){const cache=caches.default;const hour=new Date().toISOString().slice(0,13);const key=new Request(`https://melon-cache.internal/${hour}`);if(!new URL(request.url).searchParams.has('refresh')){const cached=await cache.match(key);if(cached)return cached}const upstream=await fetch(MELON,{headers:{'User-Agent':'Mozilla/5.0 (compatible; KPopYangon/1.0)','Referer':'https://www.melon.com/','Accept-Language':'ko-KR,ko;q=0.9'}});if(!upstream.ok)throw new Error(`멜론 응답 오류 (${upstream.status})`);const data=parse(await upstream.text());if(data.songs.length<90)throw new Error(`멜론 차트 분석 오류 (${data.songs.length}곡)`);const response=Response.json({...data,fetchedAt:new Date().toISOString()},{headers:{'Cache-Control':'public,max-age=300,s-maxage=3600'}});await cache.put(key,response.clone());return response}
export default{async fetch(request,env){if(new URL(request.url).pathname==='/api/melon-chart'){try{return await chart(request)}catch(error){return Response.json({error:error.message},{status:502,headers:{'Cache-Control':'no-store'}})}}return env.ASSETS.fetch(request)}};
